import { object } from 'dummy/helpers/object';
import { module, test } from 'qunit';

module('Unit | Helper | object');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = object([42]);
  assert.ok(result);
});

import { global } from 'dummy/helpers/global';
import { module, test } from 'qunit';

module('Unit | Helper | global');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = global([42]);
  assert.ok(result);
});

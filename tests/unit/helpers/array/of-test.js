import { arrayOf } from 'dummy/helpers/array/of';
import { module, test } from 'qunit';

module('Unit | Helper | array/of');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayOf([42]);
  assert.ok(result);
});

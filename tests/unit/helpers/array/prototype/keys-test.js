import { arrayPrototypeKeys } from 'dummy/helpers/array/prototype/keys';
import { module, test } from 'qunit';

module('Unit | Helper | array/prototype/keys');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayPrototypeKeys([42]);
  assert.ok(result);
});

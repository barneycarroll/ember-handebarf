import { arrayPrototypeTolocalestring } from 'dummy/helpers/array/prototype/tolocalestring';
import { module, test } from 'qunit';

module('Unit | Helper | array/prototype/tolocalestring');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayPrototypeTolocalestring([42]);
  assert.ok(result);
});

import { arrayPrototypeIndexOf } from 'dummy/helpers/array/prototype/index-of';
import { module, test } from 'qunit';

module('Unit | Helper | array/prototype/index of');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayPrototypeIndexOf([42]);
  assert.ok(result);
});

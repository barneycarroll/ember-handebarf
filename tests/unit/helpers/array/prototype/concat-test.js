import { arrayPrototypeConcat } from 'dummy/helpers/array/prototype/concat';
import { module, test } from 'qunit';

module('Unit | Helper | array/prototype/concat');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayPrototypeConcat([42]);
  assert.ok(result);
});

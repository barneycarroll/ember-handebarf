import { arrayPrototypeMap } from 'dummy/helpers/array/prototype/map';
import { module, test } from 'qunit';

module('Unit | Helper | array/prototype/map');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayPrototypeMap([42]);
  assert.ok(result);
});

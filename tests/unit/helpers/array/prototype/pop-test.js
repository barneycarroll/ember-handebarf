import { arrayPrototypePop } from 'dummy/helpers/array/prototype/pop';
import { module, test } from 'qunit';

module('Unit | Helper | array/prototype/pop');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayPrototypePop([42]);
  assert.ok(result);
});

import { arrayPrototypeEvery } from 'dummy/helpers/array/prototype/every';
import { module, test } from 'qunit';

module('Unit | Helper | array/prototype/every');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayPrototypeEvery([42]);
  assert.ok(result);
});

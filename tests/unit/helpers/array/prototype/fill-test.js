import { arrayPrototypeFill } from 'dummy/helpers/array/prototype/fill';
import { module, test } from 'qunit';

module('Unit | Helper | array/prototype/fill');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayPrototypeFill([42]);
  assert.ok(result);
});

import { arrayPrototypeIncludes } from 'dummy/helpers/array/prototype/includes';
import { module, test } from 'qunit';

module('Unit | Helper | array/prototype/includes');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayPrototypeIncludes([42]);
  assert.ok(result);
});

import { arrayPrototypeReduceRight } from 'dummy/helpers/array/prototype/reduce-right';
import { module, test } from 'qunit';

module('Unit | Helper | array/prototype/reduce right');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayPrototypeReduceRight([42]);
  assert.ok(result);
});

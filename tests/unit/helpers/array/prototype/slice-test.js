import { arrayPrototypeSlice } from 'dummy/helpers/array/prototype/slice';
import { module, test } from 'qunit';

module('Unit | Helper | array/prototype/slice');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayPrototypeSlice([42]);
  assert.ok(result);
});

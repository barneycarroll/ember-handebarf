import { arrayPrototypeShift } from 'dummy/helpers/array/prototype/shift';
import { module, test } from 'qunit';

module('Unit | Helper | array/prototype/shift');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayPrototypeShift([42]);
  assert.ok(result);
});

import { arrayPrototypeFilter } from 'dummy/helpers/array/prototype/filter';
import { module, test } from 'qunit';

module('Unit | Helper | array/prototype/filter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayPrototypeFilter([42]);
  assert.ok(result);
});

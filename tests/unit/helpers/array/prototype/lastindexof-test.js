import { arrayPrototypeLastindexof } from 'dummy/helpers/array/prototype/lastindexof';
import { module, test } from 'qunit';

module('Unit | Helper | array/prototype/lastindexof');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayPrototypeLastindexof([42]);
  assert.ok(result);
});

import { arrayPrototypeJoin } from 'dummy/helpers/array/prototype/join';
import { module, test } from 'qunit';

module('Unit | Helper | array/prototype/join');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayPrototypeJoin([42]);
  assert.ok(result);
});

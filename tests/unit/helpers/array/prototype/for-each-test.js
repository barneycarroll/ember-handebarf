import { arrayPrototypeForEach } from 'dummy/helpers/array/prototype/for-each';
import { module, test } from 'qunit';

module('Unit | Helper | array/prototype/for each');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayPrototypeForEach([42]);
  assert.ok(result);
});

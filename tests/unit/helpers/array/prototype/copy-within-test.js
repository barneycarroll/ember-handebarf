import { arrayPrototypeCopyWithin } from 'dummy/helpers/array/prototype/copy-within';
import { module, test } from 'qunit';

module('Unit | Helper | array/prototype/copy within');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayPrototypeCopyWithin([42]);
  assert.ok(result);
});

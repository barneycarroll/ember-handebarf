import { arrayPrototypePush } from 'dummy/helpers/array/prototype/push';
import { module, test } from 'qunit';

module('Unit | Helper | array/prototype/push');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayPrototypePush([42]);
  assert.ok(result);
});

import { arrayPrototypeIndexof } from 'dummy/helpers/array/prototype/indexof';
import { module, test } from 'qunit';

module('Unit | Helper | array/prototype/indexof');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayPrototypeIndexof([42]);
  assert.ok(result);
});

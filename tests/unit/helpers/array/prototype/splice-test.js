import { arrayPrototypeSplice } from 'dummy/helpers/array/prototype/splice';
import { module, test } from 'qunit';

module('Unit | Helper | array/prototype/splice');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayPrototypeSplice([42]);
  assert.ok(result);
});

import { arrayPrototypeReduce } from 'dummy/helpers/array/prototype/reduce';
import { module, test } from 'qunit';

module('Unit | Helper | array/prototype/reduce');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayPrototypeReduce([42]);
  assert.ok(result);
});

import { arrayPrototypeReverse } from 'dummy/helpers/array/prototype/reverse';
import { module, test } from 'qunit';

module('Unit | Helper | array/prototype/reverse');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayPrototypeReverse([42]);
  assert.ok(result);
});

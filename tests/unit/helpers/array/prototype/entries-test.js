import { arrayPrototypeEntries } from 'dummy/helpers/array/prototype/entries';
import { module, test } from 'qunit';

module('Unit | Helper | array/prototype/entries');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayPrototypeEntries([42]);
  assert.ok(result);
});

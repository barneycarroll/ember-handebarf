import { arrayFrom } from 'dummy/helpers/array/from';
import { module, test } from 'qunit';

module('Unit | Helper | array/from');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayFrom([42]);
  assert.ok(result);
});

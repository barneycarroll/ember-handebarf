import { arrayIsArray } from 'dummy/helpers/array/is-array';
import { module, test } from 'qunit';

module('Unit | Helper | array/is array');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayIsArray([42]);
  assert.ok(result);
});

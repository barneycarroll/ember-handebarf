import { operate } from 'dummy/helpers/operate';
import { module, test } from 'qunit';

module('Unit | Helper | operate');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = operate([42]);
  assert.ok(result);
});
